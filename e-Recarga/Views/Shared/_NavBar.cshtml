<nav class="navbar navbar-expand-md navbar-dark bg-primary sticky-top">
    <div class="container">
        @Html.ActionLink("e-Recarga", "Index", "Home", new object { }, new { @class = "navbar-brand" })


        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        @{
            if (Request.IsAuthenticated)
            {
                if (User.IsInRole("Admin"))
                {
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            @Html.ActionLink("Gerir Roles", "Index", "RolesAdmin", new object { }, new { @class = "nav-link" })
                        </li>
                        <li class="nav-item">
                            @Html.ActionLink("Gerir Utilizadores", "Index", "UsersAdmin", new object { }, new { @class = "nav-link" })
                        </li>
                    </ul>
                }
                else if (User.IsInRole("OPC"))
                {
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            @Html.ActionLink("Gerir Postos", "Index", "OPC", new object { }, new { @class = "nav-link" })
                        </li>
                    </ul>
                }
                else if (User.IsInRole("UPC"))
                {
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            @Html.ActionLink("Reservar Tomadas", "Index", "UPC", new object { }, new { @class = "nav-link" })
                        </li>
                    </ul>
                }
            }

        }

        @Html.Partial("_LoginPartial")
    </div>
</nav>
